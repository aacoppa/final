#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int digit(char c);

char rune(int c);

char* divByTwo(char* val);

int binaryDigits(int digits);

char* decToBin(char* val);

unsigned int binToDec(char* val, int start);

unsigned int* fullStoreStdin();
