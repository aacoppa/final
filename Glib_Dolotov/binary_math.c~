#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

unsigned int* add(unsigned int* a, unsigned int* b){
  int a_count = sizeof(a)/sizeof(unsigned int);
  int b_count = sizeof(b)/sizeof(unsigned int);
  for(a,b; a>0 || b>0; a--,b--){

  }
}
