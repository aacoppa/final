/* Game: 
 * -runs independently
 * -doesn't worry about client/server
 * -is called by client
 */
#ifndef __GAME_AJS
#define __GAME_AJS 1

typedef struct map {
    /* Map 
     *
     */
} map;
void startGame();
void run();
#endif
