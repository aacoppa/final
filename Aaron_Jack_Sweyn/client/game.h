/* Game: 
 * -runs independently
 * -doesn't worry about client/server
 * -is called by client
 */
#ifndef __GAME_AJS
#define __GAME_AJS 1

#include <stdlib.h>
#include <stdio.h>
typedef struct map {
    /* Map 
     *
     */
} map;
int game_start(int);
void run();
#endif
