
socket
AF_INET = ipv4
sock_stream = tcp
socket_id = socket(AF_INET, SOCK_STREAM, 0); 

//General crap above

struct sockaddr_in server;
socklen_t socket_length;

server.sin_family = AF_INET;
server.sin_port = 
//Tells the struct what kind

server.sin_addr.s_addr = INADDR_ANY; //for server
bind(socket_id, (struct sockaddr *)&server, sizeof(server));

listen(socket_id, 0); //The zero is max connections, smh

socket_length = sizeof(server);
socket_client = accept(socket_id, (struct sockaddr *)&server, &socket_length);
